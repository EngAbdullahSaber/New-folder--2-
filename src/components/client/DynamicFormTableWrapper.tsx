// 'use client'

// import { useState } from 'react'

// import DynamicFormTable from '@/shared'
// import type { FieldConfig } from '@/types/components/dynamicFormTable'

// interface WrapperProps {
//   fields: FieldConfig[]
//   initialData: any[]
// }

// const DynamicFormTableWrapper: React.FC<WrapperProps> = ({ fields, initialData }) => {
//   const [data, setData] = useState(initialData)

//   const handleDataChange = (updatedData: any[]) => {
//     setData(updatedData)
//   }

//   return <DynamicFormTable fields={fields} onDataChange={handleDataChange} title='User Accounts' initialData={data} />
// }

// export default DynamicFormTableWrapper
